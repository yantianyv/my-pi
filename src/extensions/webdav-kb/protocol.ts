/**
 * webdav-kb / protocol.ts — AI 使用守则（PROTOCOL.md 的默认内容）
 *
 * 守则本体 = 网盘根 PROTOCOL.md（跨设备同步、用户可迭代，kb_help 优先读它）；
 * 本文件是「出厂默认版」：首次使用时由 index.ts 引导写入镜像并上传网盘根，
 * 之后以网盘根版本为准（用户/多设备改动自动同步回来）。
 *
 * 指令层由用户亲自设计——这份是初稿，用户可在网盘根 PROTOCOL.md 上直接改。
 */

/** PROTOCOL.md 的默认内容（保持精炼：AI 会把它读进上下文） */
export const DEFAULT_PROTOCOL = `# 知识库使用守则（webdav-kb）

知识库 = 给 AI 用的云网盘（WebDAV 驱动，本地镜像离线可用）。AI 自发沉淀有用的东西，需要时自发检索；人类通过 /kb 面板查询引用。本守则是唯一权威规则——按它行事，用户可直接编辑本文件迭代。

## 一、命名空间（写操作只允许这四个顶层目录；路径必须分层，见「五、分类层级」）
- /notes       永久知识：技术笔记、踩坑记录、API/库用法摘要、架构决策
- /references  文档摘录：PDF/Word/网页转换的参考资料（markitdown 产出）
- /scratch     临时草稿：进行中的内容，可随时清理
- /vault       加密区：敏感内容（需口令解锁，口令只存内存，仅 kb 工具可读写）
注意：命名空间/用途下不直接放文件，必须再建至少一层自由层级（如 /references/知识文献/论文/xxx.md）。

## 二、使用流程
1. 本会话首次使用 kb_* 前，先 kb_help 读守则
2. 遇到与已有知识可能相关的问题 → 先 kb_search 再作答
3. 命中 → kb_read 读全文（长文件用 offset 续读），取最相关内容
4. 过程中产生值得沉淀的知识 → kb_write / kb_append（见下）
5. 没把握的路径 → kb_list 看现有目录结构
6. 同步状态不确定时 → kb_status（冲突/积压），必要时提醒用户 /kb-sync

## 三、触发时机（什么时候值得写）
- 解决了一个可复用的坑：写「现象 → 原因 → 解决 → 验证」，带日期与上下文
- 发现可复用的命令 / 配置 / 代码片段 / prompt 模板
- 用户明确要求记下的内容
- 参考资料摘要（对话中的长文档 → 提炼进 /references）

## 四、写入规范
- 新建前必须先 kb_search 查重；有类似笔记 → 更新旧笔记，不重复建
- 文件名：短横线连接的主题词，如 /notes/tsc-build-gotcha.md（中文主题也可）
- 支持的文本格式（纯文本可全文检索，kb_search 按内容命中）：
  - 文档类：.md / .markdown（知识库原生，**必须带 frontmatter**）、.txt（自由文本，不强制）
  - 表格类：.csv / .tsv（首行表头即列名，检索时表头加权；读全文时表头在最前）
  - 数据/配置类：.json / .jsonl / .yaml / .yml / .toml（结构即信息，全文可检索）
  - 标记文档：.html / .xml（网页原文存档等）
  - 其余纯文本（rst/org/tex/ini/log 等）暂不索引：小文件转成上述格式，大文件存 LFS
- 文档类（.md/.markdown）必须带 frontmatter：
  ---
  title: 一句话标题
  tags: [标签1, 标签2]
  ---
  正文从这里开始
- 表格/数据格式**不需要 frontmatter**（文件名即标题；csv/tsv 检索时表头加权），直接写内容即可
- 已存在文件写覆盖需 overwrite:true；补充内容优先 kb_append
- 归错类 > 不归：拿不准放 /scratch，想清楚再移

## 五、分类层级（归档路径：硬约束 + 软约束）
路径 = /命名空间/用途/自由层级/文件名，至少四段，禁止在命名空间/用途下放裸文件：
- 第 1 层 命名空间（硬约束）：/notes /references /scratch /vault
- 第 2 层 用途（硬约束）：/references 按文档功能六值判定，/notes 按知识主题类判定（见下）
- 第 3 层起 自由层级（软约束）：至少一层，由 AI 管理，层数不限；逐层选「当前集合区分度最高、最稳定的属性」下钻（体裁 → 学科/领域 → 学段 → 版本 → 主题 → 机构…），能合并为一层就不拆两层，某层下 <3 个文件则并入父层、属性下沉到文件名/tags

/references 文档功能判定（外部资料，六值互斥，判整体体裁、不看局部词汇；次要功能不参与归类，如论文含方法步骤仍是知识文献）：
- 知识/观点（论文、书、报告、讲义、新闻）→ 知识文献/
- 行为约束（法规、办法、条例、章程、合同）→ 规范文书/
- 操作指导（教程、手册、指南、说明书、流程）→ 操作指南/
- 数据查询（名录、通讯录、统计、数据）→ 数据名录/（csv/tsv 表格多归此类）
- 填写/套用（表单、模板、票据、凭证、证件）→ 表单模板/（文档本身是待使用的载体：填写/套用/备查，区别于操作指南的说明文字）
- 非文本（图、音、视、设计稿）→ 素材资源/（本体存 /lfs/）

/notes 第 2 层（自己的产出，按知识主题类判定，与文档六值无关）：
- 技术笔记/（工具用法、踩坑、配置）
- 研究笔记/（精读分析、文献综述、检索报告）
- 方法总结/（方法论、模板、复盘）
- 工作职业/（教案、总结、汇报）
- 生活管理/（健康、财务、证件）
- 兴趣创作/（阅读、旅行、创作）
主题类可随内容增补，但保持全局视角（不按项目/任务命名）

自由层级管理（AI 规则）：
- 新建层前先 kb_list 查同义复用；该主题 <3 个文件先并入相近层级，不单独建层
- 长期 <2 个文件的层级 → 并入相近层级（kb_move 批量移）
- 层级名不符实 → 重命名（kb_move）
- 每层禁止：项目名、阶段名、来源形态（markdown/ 等）、编号前缀（01-）
- 主题聚合靠 kb_search + tags（tags 记次主题/学段/来源等），不靠目录

## 六、维护
- 新信息 → 更新旧笔记（kb_append 追加「更新：…」或 kb_write 覆盖）
- 失效/错误内容 → 明确标注「已过时」而非静默删除；确需清理（写错的笔记、/scratch 草稿）用 kb_delete（必须 confirm:true，记审计日志）
- /scratch 超过一次会话的内容 → 升格到 /notes 或删除
- 发现两篇重复 → 合并到更全的一篇，另一篇标注「已合并」
- 批量迁移本地目录 → kb_import（保留源目录结构，导入后须按「五、分类层级」重新归位）

## 七、检索策略
- 检索词先具体后宽泛；没搜到就换说法或 kb_list 浏览
- 多篇冲突时按标题+片段+更新时间判断；必要时全文对比
- **搜不到就如实说搜不到，绝不编造笔记内容**

## 八、LFS 大文件区（/lfs/）
- /lfs/ 是附加的真网盘：任何类型文件都能存，不随知识库同步、不参与检索、不加密
- AI 存取走独立工具（与 md 知识库隔离）：
  - kb_upload(path, sourcePath, overwrite?)  上传：本地（相对工作目录）→ /lfs/
  - kb_download(path, destPath?, overwrite?) 下载：/lfs/ → 本地（一次性，不留缓存）
  - kb_lslfs(path?, force?)                 列出 /lfs/ 文件（路径/大小/时间）
- 上传/下载目标已存在需显式 overwrite:true；单文件上限 1GB
- md 笔记引用 lfs 文件用纯路径文本（如「附件：lfs/xxx.png」），不要用图片链接（人类环境该文件不在本地会失效）
- 人类管理 lfs：直接用 WebDAV 客户端挂载同一目录（它就是网盘）
- 敏感大文件请勿放 lfs（不加密）；文本笔记上限 50MB（kb_write 会拒绝更大内容）

## 九、vault 加密区
- 写 /vault/ 下内容需已解锁（/kb-config 输入口令）；未解锁写入会报错
- 密文只能经 kb_read/kb_write/kb_append 访问，不要尝试绕开
- 口令忘了 = 数据永久丢失（无找回机制），涉及重要内容提醒用户

## 十、人类命令（AI 可引导用户使用）
- /kb <词>      面板/文本检索 + 插入引用
- /kb-config    配置 WebDAV、vault 口令、连通测试
- /kb-sync      手动同步
`;

/** 首次同步时引导写入的提示（放在 PROTOCOL.md 前可让用户一眼看到版本说明） */
export const PROTOCOL_HEADER = `<!-- 知识库使用守则：用户可直接编辑本文件迭代；kb_help 以网盘根版本为准 -->
`;
